# The official image needs local socket access to initialize the database.
local   all   all                  trust
# Remote connections require TLS and a password; other connections are rejected.
hostssl all   all   all            scram-sha-256
